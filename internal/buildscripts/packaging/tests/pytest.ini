[pytest]
markers =
    deb
    installer
    rpm
junit_family = legacy
filterwarnings =
    ignore::DeprecationWarning:google.*:
    ignore::DeprecationWarning:sanic.*:
    ignore::DeprecationWarning:websockets.*:
